package agh.ics.oop.model.genes;

public record GenotypeInfo(int length,
                           int minGene,
                           int maxGene,
                           int minNumberOfMutations,
                           int maxNumberOfMutations
                           ) {
}
